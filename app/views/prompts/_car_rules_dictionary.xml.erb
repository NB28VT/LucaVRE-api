<car_key_mapping>
<% car_key_mapping.each do |description, key| %>
  <mapping>
    <key><%= key %></key>
    <value><%= description %></value>
  </mapping>
<% end %>
</car_key_mapping>
<engine_layout_mapping>
  <% engine_layout_mapping.each do |description, key| %>
    <mapping>
      <key><%= key %></key>
      <value><%= description %></value>
    </mapping>
  <% end %>
</engine_layout_mapping>
<aero_dependency_mapping>
  <% aero_dependency_mapping.each do |description, key| %>
    <mapping>
      <key><%= key %></key>
      <value><%= description %></value>
    </mapping>
  <% end %>
</aero_dependency_mapping>
<wheelbase_mapping>
  <% wheelbase_mapping.each do |description, key| %>
    <mapping>
      <key><%= key %></key>
      <value><%= description %></value>
    </mapping>
  <% end %>
</wheelbase_mapping>
<weight_distribution_mapping>
  <% weight_distribution_mapping.each do |description, key| %>
    <mapping>
      <key><%= key %></key>
      <value><%= description %></value>
    </mapping>
  <% end %>
</weight_distribution_mapping>
