# config/database.yml
test:
  adapter: postgresql
  encoding: unicode
  database: rails_test
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password: password
  host: localhost
