# config/database.yml
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: nathanburgess
  # password: password
  host: localhost

development:
  <<: *default
  database: lucavre_development

test:
  <<: *default
  database: lucavre_test